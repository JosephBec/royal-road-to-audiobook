# Light dependency set for CI — enough to import the app and run the
# mock-based test suite WITHOUT the heavy ML stack. torch and kokoro are
# imported lazily inside tts.py (only when synthesizing), so they're not
# needed to collect/run tests. pystray/Pillow are only used by tray.py,
# which the tests never import.
fastapi[standard]==0.115.0
uvicorn[standard]==0.30.0
sqlalchemy==2.0.32
httpx==0.27.0
beautifulsoup4==4.12.3
lxml==5.2.2
numpy==1.26.4
soundfile==0.12.1
pyyaml==6.0.2
ebooklib==0.18
python-multipart==0.0.32
pytest==8.3.2
