fastapi[standard]==0.115.0
python-multipart==0.0.32
uvicorn[standard]==0.30.0
sqlalchemy==2.0.32
httpx==0.27.0
beautifulsoup4==4.12.3
lxml==5.2.2
ebooklib==0.18
numpy==1.26.4
soundfile==0.12.1
pyyaml==6.0.2
tqdm==4.66.5
pystray==0.19.5
Pillow==10.4.0

# --- TTS engines (engines/) ---
# torch must be a CUDA build; the default PyPI wheel on Windows is CPU-only and
# silently runs ~50x slower. Install with:
#   pip install torch==2.6.0 torchaudio==2.6.0 --index-url https://download.pytorch.org/whl/cu124
torch==2.6.0
torchaudio==2.6.0

# Kokoro-82M — fast default engine
kokoro==0.9.4

# Chatterbox-Turbo — higher quality, ~29x slower than Kokoro.
# Installed with --no-deps: its metadata pins gradio and (on py>=3.13) numpy>=2,
# neither of which this app wants. Its actual runtime deps are listed below.
#   pip install --no-deps chatterbox-tts==0.1.7
chatterbox-tts==0.1.7
librosa==0.11.0
s3tokenizer
diffusers==0.29.0
resemble-perth>=1.0.0
conformer==0.3.2
omegaconf
pyloudnorm
einops
# resemble-perth imports pkg_resources, removed in setuptools 81.
setuptools<81
